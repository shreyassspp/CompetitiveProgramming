#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    string a,s;
    int t;
    cin>>t;
    while(t--)
    {
        cin>>a>>s; // String a and s are arrays.

        int i =s.size()-1;
        int j = a.size()-1;

        int s1 = int(s[i]);
        int a1 = int(a[j]);

        string b = "";bool f =1;
        while(i>=0)
        {
           
        }

    }
    return 0;
}